#include <iostream>
using namespace std;

int main() {
    int A = 5;
    int B = 10;
    // Interpretado de (A+++++B)*2
    int C = (A++ + ++B) * 2; 
    cout << A << ", " << B << ", " << C << endl;
    return 0;
}
